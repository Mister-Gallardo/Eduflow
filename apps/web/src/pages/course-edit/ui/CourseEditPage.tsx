/**
 * Страница редактирования курса.
 * Контент рендерится непосредственно в CourseEditLayout (sidebar + StepEditorBody),
 * эта страница — placeholder для route outlet.
 */
export const CourseEditPage = () => {
  return null
}
